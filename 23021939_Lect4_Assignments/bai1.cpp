*Hai tính chất quan trọng của một hàm băm là:

1. Tính phân tán tốt (Uniformity):
Hàm băm cần phân tán đầu vào một cách đồng đều trên không gian giá trị băm,
tránh tình trạng các giá trị băm bị dồn vào một số vị trí nhất định,
giúp giảm xung đột (collision) khi sử dụng trong các cấu trúc dữ liệu như bảng băm (hash table).

2. Tính kháng va chạm (Collision Resistance):
Rất khó để tìm hai đầu vào khác nhau nhưng cho cùng một giá trị băm.
Điều này đặc biệt quan trọng trong các ứng dụng bảo mật, như chữ ký số và mã hóa dữ liệu.
